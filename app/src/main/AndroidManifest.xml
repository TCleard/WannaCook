<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tcleard.wannacook">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".scene.main.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity
            android:name=".scene.recipe.DetailRecipeActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".scene.edit.EditRecipeActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".scene.ingredient.IngredientActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>
